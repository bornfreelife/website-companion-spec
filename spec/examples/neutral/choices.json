{
  "schemaVersion": "1.0",
  "resourceType": "choices",
  "resourceId": "community-events.choices",
  "version": "2026.08.28.1",
  "choiceSets": [
    {
      "choiceId": "attendance-region",
      "title": "Which event region should be used?",
      "input": "single",
      "required": true,
      "localOnly": true,
      "default": "DEMO",
      "options": [
        { "value": "DEMO", "label": "Demonstration region" },
        { "value": "REMOTE", "label": "Remote region" }
      ]
    },
    {
      "choiceId": "event-interests",
      "title": "Which event types interest you?",
      "description": "Selections stay on this device and filter the public event checklist locally.",
      "input": "multiple",
      "required": false,
      "localOnly": true,
      "default": ["workshop"],
      "options": [
        { "value": "workshop", "label": "Workshops" },
        { "value": "market", "label": "Markets" }
      ]
    }
  ]
}
